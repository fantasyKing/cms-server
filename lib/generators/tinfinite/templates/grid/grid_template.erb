class <%= class_name %>Grid

  include Datagrid

  scope do
    <%= class_name %>
  end

  # column(:app_id)

  # filter(:app_id, :string)

  column(:actions, :html => true) do |obj|
    render :partial => "db_<%= db_name %>/<%= model_name.pluralize %>/actions", :locals => {obj: obj}
  end
end
